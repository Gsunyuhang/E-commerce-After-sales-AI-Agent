测试按被测模块一一对应组织为三个文件：`test_agent.py` 验证 `agent.intent` 常量、`agent.graph` 的路由与校验分支以及 `config.prompts` 的模板格式化；`test_retriever.py` 验证 `knowledge.document_loader.DocumentLoader` 的加载/分块能力与 `config.settings` 配置项；`test_tools.py` 通过 `database.init_db.init_database(reset=True)` 在模块级 fixture 中初始化 SQLite 数据库并预置订单数据，再对 `tools.order_query`、`tools.logistics_query`、`tools.ticket_create`、`tools.refund_calc` 四个工具执行端到端调用断言。所有测试文件均通过 `sys.path.insert(0, ...)` 将仓库根加入路径以直接 import 同级包，无额外测试框架或配置文件。