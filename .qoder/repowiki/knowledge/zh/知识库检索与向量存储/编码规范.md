- 对外暴露全局单例工厂函数（`get_vector_store_manager`、`get_retriever`），内部再用类级 `_instance` 做二次守卫，避免重复初始化。
- 所有 I/O 与 LLM 调用被 try/except 包裹，捕获后记录日志并抛出 `RetrievalError`，确保上层只感知业务异常。
- 配置全部经由 `config.settings.get_settings()` 获取，不在本模块内硬编码路径、阈值或集合名。